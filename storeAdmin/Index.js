import "./components/login.js"
import "./components/Usuarios.js"
import "./components/Productos.js"
import "./components/Categorias.js"
import "./components/Compras.js"
import "./components/Marcas.js"

document.addEventListener('DOMContentLoaded', () => { });
window.addEventListener('load', () => {
    
    /*const content = document.querySelector("#content");

     content.insertAdjacentHTML('beforeend', '<comp-usuarios></comp-usuarios>');

    document.addEventListener('click', async (e) => {

        const btnElement = e.target.closest('button');

        if (btnElement) {

            if (btnElement.id === 'user') {
                content.firstElementChild.remove();
                content.insertAdjacentHTML('beforeend', '<comp-usuarios></comp-usuarios>');
            }
            if (btnElement.id === 'marca') {
                content.firstElementChild.remove();
                content.insertAdjacentHTML('beforeend', '<comp-marcas></comp-marcas>');
            }
            if (btnElement.id === 'category') {
                content.firstElementChild.remove();
                content.insertAdjacentHTML('beforeend', '<comp-categorias></comp-categorias>');
            }
            if (btnElement.id === 'product') {
                content.firstElementChild.remove();
                content.insertAdjacentHTML('beforeend', '<comp-productos></comp-productos>');
            }
            if (btnElement.id === 'shops') {
                content.firstElementChild.remove();
                content.insertAdjacentHTML('beforeend', '<comp-compras></comp-compras>');
            }

        }

    });

    const buttons = document.querySelectorAll(".button-menu");
    const userButton = document.getElementById("user");

    userButton.style.backgroundColor = "#034780";
    userButton.style.color = "#ffffff";

    buttons.forEach((button) => {
        button.addEventListener("click", () => {
            buttons.forEach((btn) => {
                btn.style.backgroundColor = "#ffffff";
                btn.style.color = "#00193A";
            });

            button.style.backgroundColor = "#034780";
            button.style.color = "#ffffff";
        });
    }); */

});